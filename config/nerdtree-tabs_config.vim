map <silent><c-d> <plug>NERDTreeTabsToggle<CR>

let g:nerdtree_tabs_open_on_console_startup = 0 "open nerdtree on console vim startup
let g:nerdtree_tabs_autofind = 1 "automatically find and select currently openend file in NERDTree
let g:nerdtree_tabs_focus_on_files = 1 "when switching into a tab, make sure that focus is on file window and not in the NERDTree window
