let g:airline#extensions#tabline#enabled = 1 "automatically display all buffers when there's only one tab open
let g:airline#extensions#whitespace#enabled = 1 "automatically show if mixed/trailing whitespaces
let g:airline_powerline_fonts = 1 "allow airline to use powerline font

set laststatus=2 "allow the status bar to appear all the time
"themes for airline
Plug 'vim-airline/vim-airline-themes'

"let g:airline_theme = 'dark theme with powerline symbols' "theme used
